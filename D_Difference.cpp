#include <bits/stdc++.h>
using namespace std;

int main() 
{
    long long int A, B, C, D,X;
    cin >> A >> B >> C >> D;
    X = ((A*B) - (C*D));
    cout << "Difference = " << X << endl;
    return 0;
}
